<% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this obj from being saved:
        <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
<h1>Editing Book</h1>
<p id="notice"></p>
  <div class='container'>
    <div class='row'>
      <h2>Book info</h2>
      <%= form_with model: @book do |f| %>
      <label for="inputTitle">Title</label>
      <%= f.text_field :title, autofoucus: true, id:"inputTitle", placeholder:"title"%>
      <label for="inputOpinion">Opinion</label>
      <%= f.text_area :body, autofoucus: true, id:"inputOpinion", placeholder:"body"%>
      <%= f.submit "Update Book"%>
      <% end %>
    </div>
  </div>
<%= link_to"Show",book_path %>
|
<%= link_to"Back",'/books' %>
